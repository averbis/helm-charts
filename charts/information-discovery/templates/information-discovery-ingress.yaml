apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: id-ingress  
  annotations:
    kubernetes.io/ingress.class: gce

spec:
  defaultBackend:
    service:
      name: id-node-port
      port:
        number: 8080